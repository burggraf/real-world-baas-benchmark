import test from "node:test";
import assert from "node:assert/strict";
import { createFakeBackend } from "./fake-backend.js";
import { runCorrectness } from "../src/correctness.js";
test("secure fake passes and insecure tenant isolation is found",async()=>{const insecure=createFakeBackend({insecureTenantIsolation:true});const bad=await runCorrectness(insecure,insecure.fixture);assert.equal(bad.findings.find(x=>x.name==="outsider-read-isolated")?.passed,false);const secure=createFakeBackend();const good=await runCorrectness(secure,secure.fixture);assert.equal(good.aborted,false);assert.equal(good.findings.some(x=>!x.passed),false);assert.equal(new Set(good.findings.map(x=>x.name)).size,good.findings.length);assert.equal(secure.closedSessions,3);});
test("malformed and application failures are classified and continue",async()=>{const b=createFakeBackend({failures:{malformed:1}});const r=await runCorrectness(b,b.fixture);assert.equal(r.findings.find(x=>x.name==="profile-read-update")?.classification,"invalid_response");assert.ok(r.findings.some(x=>x.name==="required-data"));});
test("backend health loss aborts",async()=>{const b=createFakeBackend({failures:{backend_health:1}});const r=await runCorrectness(b,b.fixture);assert.equal(r.aborted,true);assert.ok(r.findings.length<12);});
test("application and timeout failures are classified while later checks continue",async()=>{for(const kind of ["application","timeout"] as const){const b=createFakeBackend({failures:{[kind]:1}});const r=await runCorrectness(b,b.fixture);assert.ok(r.findings.some(x=>x.classification===kind));assert.ok(r.findings.some(x=>x.name==="required-data"));}});
test("authentication failure is classified and suite continues",async()=>{const b=createFakeBackend({failures:{authentication:1}});const r=await runCorrectness(b,b.fixture);assert.equal(r.findings[0]?.classification,"authentication");assert.ok(r.findings.some(x=>x.name==="required-data"));});
test("results never contain passwords",async()=>{const b=createFakeBackend();const r=await runCorrectness(b,b.fixture);const text=JSON.stringify(r);assert.equal(text.includes("owner-pass"),false);assert.equal(text.includes("member-pass"),false);});
