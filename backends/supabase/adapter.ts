import { createClient, type SupabaseClient } from "@supabase/supabase-js";
import type { Backend, AppSession, BackendInfo } from "../../src/backend.js";
import type { Credentials, DatasetProfile, Page } from "../../src/domain.js";
import { BenchmarkOperationError } from "../../src/correctness.js";
import { supabaseProcess } from "./process.js";
export function normalizeSupabaseError(error:unknown,status?:number):BenchmarkOperationError { const e=error as any; const s=status??(typeof e?.status==="number"?e.status:undefined); return new BenchmarkOperationError(s===401?"authentication":s===403?"authorization":s===408?"timeout":"transport/sdk",{status:s,code:typeof e?.code==="string"?e.code:"supabase_error"}); }
export function pageRange(page:number,pageSize:number):[number,number] { if(!Number.isInteger(page)||page<0||!Number.isInteger(pageSize)||pageSize<=0) throw new BenchmarkOperationError("invalid_response",{code:"pagination"}); return [page*pageSize,page*pageSize+pageSize-1]; }
export function createSupabaseClient(url:string,key:string,projectId:string):SupabaseClient { return createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false,detectSessionInUrl:false,storageKey:`local-${projectId}-auth`}}); }
const fields={profiles:"id,email,display_name,created_at,updated_at",organizations:"id,name,owner_id,created_at",memberships:"id,organization_id,user_id,role,created_at",projects:"id,organization_id,name,status,created_at,updated_at",tasks:"id,project_id,creator_id,assignee_id,title,description,status,priority,due_date,created_at,updated_at",comments:"id,task_id,author_id,body,created_at,updated_at",activities:"id,organization_id,project_id,actor_id,action,subject_type,subject_id,created_at"};
const map=(x:any)=>({id:x.id,email:x.email,displayName:x.display_name,createdAt:x.created_at,updatedAt:x.updated_at});
function result(r:any){if(r.error)throw normalizeSupabaseError(r.error,r.status);return r.data;}
class Session implements AppSession { constructor(private c:SupabaseClient,private profile:any){} async getProfile(){return map(this.profile)} async refreshSession(){const r=await this.c.auth.refreshSession();result(r)} async signOut(){result(await this.c.auth.signOut())} async close(){await this.signOut().catch(()=>{})}
 async dashboard(i:any){const [o,p,a]=await Promise.all([this.c.from("organizations").select(fields.organizations).eq("id",i.organizationId).single(),this.c.from("projects").select(fields.projects).eq("organization_id",i.organizationId).order("id").range(0,999),this.c.from("activities").select(fields.activities).eq("organization_id",i.organizationId).order("created_at",{ascending:false}).range(...pageRange(i.activityPage?.page??0,i.activityPage?.pageSize??10))]); return {organization:result(o),projects:result(p).map((x:any)=>({...x,organizationId:x.organization_id,createdAt:x.created_at,updatedAt:x.updated_at})),recentActivity:result(a)} }
 async listTasks(i:any){let q=this.c.from("tasks").select(fields.tasks,{count:"exact"}).eq("project_id",i.projectId).order("created_at").order("id");if(i.status)q=q.eq("status",i.status);if(i.assigneeId===null)q=q.is("assignee_id",null);else if(i.assigneeId)q=q.eq("assignee_id",i.assigneeId);const r:any=await q.range(...pageRange(i.page,i.pageSize));const d=result(r)??[];return {items:d.map(task),page:i.page,pageSize:i.pageSize,total:r.count??d.length,hasNext:(i.page+1)*i.pageSize<(r.count??d.length)} }
 async searchTasks(i:any){return this.listTasks({...i,status:undefined,query:i.query})}
 async getTask(i:any){const r:any=await this.c.from("tasks").select(fields.tasks).eq("id",i.taskId).eq("project_id",i.projectId).single();const row=result(r);const c:any=await this.c.from("comments").select(fields.comments,{count:"exact"}).eq("task_id",i.taskId).order("created_at").range(...pageRange(i.comments.page,i.comments.pageSize));return {task:task(row),creator:map(await profile(this.c,row.creator_id)),assignee:row.assignee_id?map(await profile(this.c,row.assignee_id)):null,comments:{items:result(c)??[],page:i.comments.page,pageSize:i.comments.pageSize,total:c.count??0,hasNext:(i.comments.page+1)*i.comments.pageSize<(c.count??0)}}}
 async createTask(i:any){const r=await this.c.from("tasks").insert({id:crypto.randomUUID().replaceAll("-","").slice(0,15),project_id:i.projectId,creator_id:this.profile.id,assignee_id:i.assigneeId??null,title:i.title,description:i.description,status:"todo",priority:i.priority,due_date:i.dueDate??null}).select(fields.tasks).single();return task(result(r))}
 async updateTask(i:any){const {taskId,organizationId,projectId,...patch}=i;const r=await this.c.from("tasks").update(Object.fromEntries(Object.entries(patch).map(([k,v])=>[({dueDate:"due_date"}[k]??k),v]))).eq("id",taskId).eq("project_id",projectId).select(fields.tasks).single();return task(result(r))}
 async addComment(i:any){const r=await this.c.from("comments").insert({id:crypto.randomUUID().replaceAll("-","").slice(0,15),task_id:i.taskId,author_id:this.profile.id,body:i.body}).select(fields.comments).single();return comment(result(r))}
 async updateComment(i:any){const r=await this.c.from("comments").update({body:i.body}).eq("id",i.commentId).eq("task_id",i.taskId).select(fields.comments).single();return comment(result(r))}
 async updateMembershipRole(i:any){const r=await this.c.from("memberships").update({role:i.role}).eq("id",i.membershipId).eq("organization_id",i.organizationId).select(fields.memberships).single();const x=result(r);return {...x,organizationId:x.organization_id,userId:x.user_id,createdAt:x.created_at}}
 async updateProfile(i:any){const r=await this.c.from("profiles").update({display_name:i.displayName}).eq("id",this.profile.id).select(fields.profiles).single();this.profile=result(r);return map(this.profile)} }
function task(x:any){return {...x,projectId:x.project_id,creatorId:x.creator_id,assigneeId:x.assignee_id, dueDate:x.due_date,createdAt:x.created_at,updatedAt:x.updated_at}}
function comment(x:any){return {...x,taskId:x.task_id,authorId:x.author_id,createdAt:x.created_at,updatedAt:x.updated_at}}
async function profile(c:any,id:string){return result(await c.from("profiles").select(fields.profiles).eq("id",id).single())}
export const backend:Backend={name:"supabase",doctor:async():Promise<BackendInfo>=>supabaseProcess.doctor(),start:()=>supabaseProcess.start(),reset:()=>supabaseProcess.reset(),stop:()=>supabaseProcess.stop(),seed:async()=>{throw new BenchmarkOperationError("transport/sdk",{code:"seed_requires_stack"})},createSession:async(credentials:Credentials)=>{const s=await supabaseProcess.status();const c=createSupabaseClient(String(s.API_URL),String(s.PUBLISHABLE_KEY??s.ANON_KEY),s.project_id as string??"realworldbaasbench");const r=await c.auth.signInWithPassword(credentials);const u=result(r).user;if(!u)throw new BenchmarkOperationError("authentication",{code:"missing_user"});const pr=result(await c.from("profiles").select(fields.profiles).eq("auth_id",u.id).single()); return new Session(c,pr);}};
export const mapSupabaseTask = task;
export const mapSupabaseComment = comment;
export const mapSupabasePage = <T>(items:T[]|null,page:number,pageSize:number,total:number):Page<T> => ({items:items??[],page,pageSize,total,hasNext:(page+1)*pageSize<total});
