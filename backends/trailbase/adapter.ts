import { initClient, type Client, type FilterOrComposite } from "trailbase";
import type { Backend, AppSession, BackendInfo } from "../../src/backend.js";
import type { Activity, AddCommentInput, Comment, CreateTaskInput, Credentials, Dashboard, DashboardInput, DatasetProfile, GetTaskInput, ListTasksInput, Membership, Organization, Page, Project, Role, SearchTasksInput, Task, TaskDetail, TaskPriority, TaskStatus, UpdateCommentInput, UpdateMembershipRoleInput, UpdateProfileInput, User } from "../../src/domain.js";
import { BenchmarkOperationError, type CorrectnessFixture } from "../../src/correctness.js";
import { datasetProfiles, entityId, seedDataset, type EntityName, type ProfileName, type SeedRecord } from "../../src/seed.js";
import { LOCAL_BENCHMARK_PASSWORD, LOCAL_SETUP_EMAIL, LOCAL_SETUP_PASSWORD, trailBaseProcess, resolveTrailBaseOptions, TRAILBASE_VERSION } from "./process.js";

type Row = Record<string, unknown>;
const BATCH = 50;
const IDs = { owner:"fxown0000000001", admin:"fxadm0000000001", member:"fxmem0000000001", outsider:"fxout0000000001", organization:"fxorg0000000001", ownerMembership:"fxmow0000000001", adminMembership:"fxmad0000000001", memberMembership:"fxmme0000000001", project:"fxprj0000000001", task:"fxtsk0000000001", secondOrganization:"fxorg0000000002", secondAdminMembership:"fxmad0000000002", outsiderMembership:"fxmou0000000002" } as const;
export interface TrailBaseCorrectnessFixture extends CorrectnessFixture { foreignMembershipId:string; outsiderUserId:string; }

function row(v: unknown): Row { if (!v || typeof v !== "object" || Array.isArray(v)) throw new BenchmarkOperationError("invalid_response", {code:"record_shape"}); return v as Row; }
function str(r:Row,k:string):string { const v=r[k]; if(typeof v!=="string"||!v) throw new BenchmarkOperationError("invalid_response",{code:"record_field"}); return v; }
function text(r:Row,k:string):string { const v=r[k]; if(typeof v!=="string") throw new BenchmarkOperationError("invalid_response",{code:"record_field"}); return v; }
function publicId(r:Row):string { return typeof r.publicId==="string"&&r.publicId ? r.publicId : str(r,"id"); }
function opt(r:Row,k:string):string|null { const v=r[k]; if(v===null||v===undefined||v==="") return null; if(typeof v!=="string") throw new BenchmarkOperationError("invalid_response",{code:"record_field"}); return v; }
function en<T extends string>(r:Row,k:string,a:readonly T[]):T { const v=str(r,k); if(!a.includes(v as T)) throw new BenchmarkOperationError("invalid_response",{code:"record_enum"}); return v as T; }
export function mapTrailBaseUser(v:unknown):User { const r=row(v); return {id:publicId(r),email:str(r,"email"),displayName:text(r,"displayName"),createdAt:str(r,"createdAt"),updatedAt:str(r,"updatedAt")}; }
export function mapTrailBaseTask(v:unknown):Task { const r=row(v); return {id:publicId(r),projectId:str(r,"projectId"),creatorId:str(r,"creatorId"),assigneeId:opt(r,"assigneeId"),title:text(r,"title"),description:text(r,"description"),status:en<TaskStatus>(r,"status",["todo","in_progress","done","cancelled"]),priority:en<TaskPriority>(r,"priority",["low","medium","high","urgent"]),dueDate:opt(r,"dueDate"),createdAt:str(r,"createdAt"),updatedAt:str(r,"updatedAt")}; }
function org(v:unknown):Organization { const r=row(v); return {id:publicId(r),name:str(r,"name"),ownerId:str(r,"ownerId"),createdAt:str(r,"createdAt")}; }
function project(v:unknown):Project { const r=row(v); return {id:publicId(r),organizationId:str(r,"organizationId"),name:str(r,"name"),status:str(r,"status"),createdAt:str(r,"createdAt"),updatedAt:str(r,"updatedAt")}; }
function comment(v:unknown):Comment { const r=row(v); return {id:publicId(r),taskId:str(r,"taskId"),authorId:str(r,"authorId"),body:str(r,"body"),createdAt:str(r,"createdAt"),updatedAt:str(r,"updatedAt")}; }
function activity(v:unknown):Activity { const r=row(v); return {id:publicId(r),organizationId:str(r,"organizationId"),projectId:opt(r,"projectId"),actorId:str(r,"actorId"),action:str(r,"action"),subjectType:str(r,"subjectType"),subjectId:str(r,"subjectId"),createdAt:str(r,"createdAt")}; }
function membership(v:unknown):Membership { const r=row(v); return {id:publicId(r),organizationId:str(r,"organizationId"),userId:str(r,"userId"),role:en<Role>(r,"role",["owner","admin","member"]),createdAt:str(r,"createdAt")}; }
export function normalizeTrailBaseError(e:unknown):BenchmarkOperationError { const status=typeof e==="object"&&e!==null&&"status" in e?(e as {status?:unknown}).status:undefined; return new BenchmarkOperationError(status===401?"authentication":status===403?"authorization":"transport/sdk",{code:"trailbase_request",status:typeof status==="number"?status:undefined}); }
export function trailBaseTaskFilters(input:ListTasksInput|SearchTasksInput):FilterOrComposite[] { const f:FilterOrComposite[]=[{column:"organizationId",op:"equal",value:input.organizationId},{column:"projectId",op:"equal",value:input.projectId}]; if("status" in input&&input.status!==undefined) f.push({column:"status",op:"equal",value:input.status}); if("assigneeId" in input&&input.assigneeId!==undefined) f.push(input.assigneeId?{column:"assigneeId",op:"equal",value:input.assigneeId}:{column:"assigneeId",op:"isNull",value:""}); if("query" in input&&input.query) f.push({or:[{column:"title",op:"like",value:`%${input.query}%`},{column:"description",op:"like",value:`%${input.query}%`}]}); return f; }

async function call<T>(fn:()=>Promise<T>):Promise<T>{try{return await fn();}catch(e){throw normalizeTrailBaseError(e);}}
async function list<T>(client:Client,name:string,opts:Parameters<ReturnType<Client["records"]>["list"]>[0]):Promise<Page<T>> { const r=await call(()=>client.records<T>(name).list({...opts,pagination:{limit:Math.min(100,Math.max(1,opts?.pagination?.limit||50)),offset:Math.max(0,opts?.pagination?.offset||0)}})); const pageSize=opts?.pagination?.limit||50; const offset=opts?.pagination?.offset||0; return {items:r.records,page:Math.floor(offset/pageSize),pageSize,total:r.total_count??r.records.length,hasNext:r.records.length===pageSize}; }
function idOf(v:unknown):string { if(typeof v!=="string"&&typeof v!=="number") throw new BenchmarkOperationError("invalid_response",{code:"record_id"}); return String(v); }

class TrailBaseSession implements AppSession {
  private closed=false;
  constructor(private readonly client:Client){}
  private user():User { if(this.closed) throw new BenchmarkOperationError("authentication",{code:"signed_out"}); const u=this.client.user(); if(!u) throw new BenchmarkOperationError("authentication",{code:"signed_out"}); const r=u as unknown as Row; return {id:str(r,"id"),email:str(r,"email"),displayName:String(r.displayName??r.username??""),createdAt:String(r.createdAt??""),updatedAt:String(r.updatedAt??"")}; }
  private async own(orgId:string){const u=this.user(); const p=await list<Row>(this.client,"memberships",{pagination:{limit:1,offset:0},filters:[{and:[{column:"organizationId",op:"equal",value:orgId},{column:"userId",op:"equal",value:u.id}]}]}); if(!p.items.length) throw new BenchmarkOperationError("authorization",{code:"tenant_denied"}); return u;}
  private async task(input:{organizationId:string;projectId:string;taskId:string}){await this.own(input.organizationId); const p=await list<Row>(this.client,"tasks",{pagination:{limit:1,offset:0},filters:[{and:[{column:"publicId",op:"equal",value:input.taskId},{column:"organizationId",op:"equal",value:input.organizationId},{column:"projectId",op:"equal",value:input.projectId}]}]}); if(!p.items.length) throw new BenchmarkOperationError("authorization",{code:"task_denied"}); return p.items[0]!;}
  async dashboard(i:DashboardInput):Promise<Dashboard>{await this.own(i.organizationId); const [o,ps,a]=await Promise.all([list<Row>(this.client,"organizations",{pagination:{limit:1},filters:[{column:"publicId",op:"equal",value:i.organizationId}]}),list<Row>(this.client,"projects",{pagination:{limit:100,offset:0},order:["createdAt","publicId"],filters:[{column:"organizationId",op:"equal",value:i.organizationId}]}),list<Row>(this.client,"activities",{pagination:{limit:i.activityPage?.pageSize||10,offset:(i.activityPage?.page||0)*(i.activityPage?.pageSize||10)},order:["-createdAt","-publicId"],filters:[{column:"organizationId",op:"equal",value:i.organizationId}]})]); if(!o.items[0]) throw new BenchmarkOperationError("invalid_response",{code:"organization"}); return {organization:org(o.items[0]),projects:ps.items.map(project),recentActivity:a.items.map(activity)};}
  async listTasks(i:ListTasksInput):Promise<Page<Task>>{await this.own(i.organizationId); const p=await list<Row>(this.client,"tasks",{pagination:{limit:i.pageSize,offset:i.page*i.pageSize},order:["createdAt","publicId"],filters:trailBaseTaskFilters(i)}); return {...p,items:p.items.map(mapTrailBaseTask)};}
  async getTask(i:GetTaskInput):Promise<TaskDetail>{const t=await this.task(i); const [cs,c]=await Promise.all([list<Row>(this.client,"comments",{pagination:{limit:i.comments.pageSize,offset:i.comments.page*i.comments.pageSize},order:["createdAt","publicId"],filters:[{and:[{column:"taskId",op:"equal",value:i.taskId},{column:"organizationId",op:"equal",value:i.organizationId},{column:"projectId",op:"equal",value:i.projectId}]}]}),call(()=>this.client.records<Row>("users").read(str(t,"creatorId"))) ]); const assignee=opt(t,"assigneeId"); return {task:mapTrailBaseTask(t),creator:mapTrailBaseUser(c),assignee:assignee?mapTrailBaseUser(await call(()=>this.client.records<Row>("users").read(assignee))):null,comments:{...cs,items:cs.items.map(comment)}};}
  async createTask(i:CreateTaskInput):Promise<Task>{const u=await this.own(i.organizationId); const r:Row={publicId:crypto.randomUUID().replaceAll("-","").slice(0,15),organizationId:i.organizationId,projectId:i.projectId,creatorId:u.id,assigneeId:i.assigneeId??null,title:i.title,description:i.description,status:"todo",priority:i.priority,dueDate:i.dueDate??null}; const id=await call(()=>this.client.records<Row>("tasks").create(r)); return mapTrailBaseTask(await call(()=>this.client.records<Row>("tasks").read(id)));}
  async updateTask(i:import("../../src/domain.js").UpdateTaskInput):Promise<Task>{const old=(await this.task(i))!; const patch:Row={}; for(const k of ["status","priority","assigneeId","dueDate","title","description"] as const) if(i[k]!==undefined) patch[k]=i[k]; await call(()=>this.client.records<Row>("tasks").update(idOf(old.publicId??old.id),patch)); return mapTrailBaseTask(await call(()=>this.client.records<Row>("tasks").read(idOf(old.publicId??old.id))));}
  async addComment(i:AddCommentInput):Promise<Comment>{const u=await this.own(i.organizationId); await this.task(i); const r:Row={publicId:crypto.randomUUID().replaceAll("-","").slice(0,15),organizationId:i.organizationId,projectId:i.projectId,taskId:i.taskId,authorId:u.id,body:i.body}; const id=await call(()=>this.client.records<Row>("comments").create(r)); return comment(await call(()=>this.client.records<Row>("comments").read(id)));}
  async updateComment(i:UpdateCommentInput):Promise<Comment>{await this.own(i.organizationId); const p=await list<Row>(this.client,"comments",{pagination:{limit:1},filters:[{and:[{column:"publicId",op:"equal",value:i.commentId},{column:"taskId",op:"equal",value:i.taskId},{column:"organizationId",op:"equal",value:i.organizationId}]}]}); if(!p.items[0]) throw new BenchmarkOperationError("authorization",{code:"comment_denied"}); const target=p.items[0]!; await call(()=>this.client.records<Row>("comments").update(idOf(target.publicId??target.id),{body:i.body})); return comment(await call(()=>this.client.records<Row>("comments").read(idOf(target.publicId??target.id))));}
  async updateMembershipRole(i:UpdateMembershipRoleInput):Promise<Membership>{const u=await this.own(i.organizationId); const me=await list<Row>(this.client,"memberships",{pagination:{limit:1},filters:[{and:[{column:"organizationId",op:"equal",value:i.organizationId},{column:"userId",op:"equal",value:u.id}]}]}); if(!me.items[0]||!(["owner","admin"] as string[]).includes(String(me.items[0].role))) throw new BenchmarkOperationError("authorization",{code:"role_denied"}); const target=await call(()=>this.client.records<Row>("memberships").read(i.membershipId)); if(String(target.organizationId)!==i.organizationId) throw new BenchmarkOperationError("authorization",{code:"membership_tenant_denied"}); await call(()=>this.client.records<Row>("memberships").update(i.membershipId,{role:i.role})); return membership({...target,role:i.role});}
  async searchTasks(i:SearchTasksInput):Promise<Page<Task>>{return this.listTasks(i as ListTasksInput);}
  async getProfile():Promise<User>{const u=this.user(); return mapTrailBaseUser(await call(()=>this.client.records<Row>("profiles").read(u.id)));}
  async updateProfile(i:UpdateProfileInput):Promise<User>{const u=this.user(); await call(()=>this.client.records<Row>("profiles").update(u.id,{displayName:i.displayName})); return this.getProfile();}
  async refreshSession(){if(this.closed) throw new BenchmarkOperationError("authentication",{code:"signed_out"}); await call(()=>this.client.refreshAuthToken({force:true}));}
  async signOut(){if(!this.closed) await call(()=>this.client.logout());}
  async close(){if(!this.closed){await this.signOut();this.closed=true;}}
}

async function register(client:Client,email:string,password:string){ const base=String(client.base); const response=await fetch(new URL("/api/auth/v1/register",base),{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password,password_repeat:password})}); if(!response.ok) throw new BenchmarkOperationError("transport/sdk",{code:"setup_registration",status:response.status}); }
async function setupClient():Promise<Client>{ const client=initClient(resolveTrailBaseOptions().endpoint); try { await call(()=>client.login(LOCAL_SETUP_EMAIL,LOCAL_SETUP_PASSWORD)); } catch { await register(client,LOCAL_SETUP_EMAIL,LOCAL_SETUP_PASSWORD); await call(()=>client.login(LOCAL_SETUP_EMAIL,LOCAL_SETUP_PASSWORD)); } return client; }
async function provisionUser(client:Client,email:string){ await register(client,email,LOCAL_BENCHMARK_PASSWORD); }
function seedRow(batch:SeedRecord):Row { const r={...(batch as unknown as Row)}; if("id" in r){r.publicId=r.id; delete r.id;} return r; }
export async function seedTrailBaseCorrectnessFixture():Promise<TrailBaseCorrectnessFixture>{
  const c=await setupClient(); const users=["owner","admin","member","outsider"] as const;
  for(const name of users) await provisionUser(c,`${name}@trailbase.bench.test`);
  const now=new Date().toISOString(); const api=(n:string)=>c.records<Row>(n); const put=async(n:string,r:Row)=>{await call(()=>api(n).create(r));};
  for(const [id,name] of [[IDs.owner,"owner"],[IDs.admin,"admin"],[IDs.member,"member"],[IDs.outsider,"outsider"]] as const) await put("users",{publicId:id,email:`${name}@trailbase.bench.test`,displayName:name,createdAt:now,updatedAt:now});
  await put("organizations",{publicId:IDs.organization,name:"Fixture organization",ownerId:IDs.owner,createdAt:now}); await put("organizations",{publicId:IDs.secondOrganization,name:"Foreign organization",ownerId:IDs.outsider,createdAt:now});
  for(const [id,user,role] of [[IDs.ownerMembership,IDs.owner,"owner"],[IDs.adminMembership,IDs.admin,"admin"],[IDs.memberMembership,IDs.member,"member"],[IDs.outsiderMembership,IDs.outsider,"admin"]] as const) await put("memberships",{publicId:id,organizationId:id===IDs.outsiderMembership?IDs.secondOrganization:IDs.organization,userId:user,role,createdAt:now});
  await put("projects",{publicId:IDs.project,organizationId:IDs.organization,name:"Fixture project",status:"active",createdAt:now,updatedAt:now}); await put("tasks",{publicId:IDs.task,organizationId:IDs.organization,projectId:IDs.project,creatorId:IDs.owner,assigneeId:IDs.member,title:"Fixture task",description:"Fixture",status:"todo",priority:"medium",dueDate:null,createdAt:now,updatedAt:now});
  await c.logout(); return {owner:{email:"owner@trailbase.bench.test",password:LOCAL_BENCHMARK_PASSWORD},admin:{email:"admin@trailbase.bench.test",password:LOCAL_BENCHMARK_PASSWORD},member:{email:"member@trailbase.bench.test",password:LOCAL_BENCHMARK_PASSWORD},outsider:{email:"outsider@trailbase.bench.test",password:LOCAL_BENCHMARK_PASSWORD},organizationId:IDs.organization,projectId:IDs.project,taskId:IDs.task,ownerMembershipId:IDs.ownerMembership,adminMembershipId:IDs.adminMembership,memberMembershipId:IDs.memberMembership,memberUserId:IDs.member,foreignMembershipId:IDs.outsiderMembership,outsiderUserId:IDs.outsider};
}
export async function seedTrailBase(profile:DatasetProfile,seed:number):Promise<void>{ const c=await setupClient(); const seen:Record<string,number>={}; try { for await(const batch of seedDataset(profile.name,seed,BATCH)){ const rows=batch.records.map(seedRow); for(let i=0;i<rows.length;i+=BATCH){await call(()=>c.records<Row>(batch.entity+"s").createBulk(rows.slice(i,i+BATCH)));} seen[batch.entity]=(seen[batch.entity]||0)+rows.length; } for(const [e,n] of Object.entries(seen)){const got=await list<Row>(c,e+"s",{pagination:{limit:1,offset:0},count:true}); if(got.total!==n) throw new BenchmarkOperationError("invalid_response",{code:"seed_count_mismatch"});} } finally { await c.logout(); } }
export const backend:Backend={name:"trailbase",doctor:()=>trailBaseProcess.doctor(),start:()=>trailBaseProcess.start(),reset:()=>trailBaseProcess.reset(),stop:()=>trailBaseProcess.stop(),seed:seedTrailBase,createSession:async(c:Credentials)=>{const client=initClient(resolveTrailBaseOptions().endpoint); await call(()=>client.login(c.email,c.password)); return new TrailBaseSession(client);}};
