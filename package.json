{
  "name": "bench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "bench": "npm run build --silent && node dist/src/cli.js",
    "download-backends": "node scripts/download-backends.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.3",
    "pocketbase": "0.28.0",
    "trailbase": "0.14.0"
  }
}
