import type { Backend, AppSession } from "./backend.js";
import type { Credentials, Id, Role } from "./domain.js";
import type { FindingClassification, CorrectnessFinding } from "./result.js";
export interface CorrectnessFixture { owner: Credentials; admin?: Credentials; member: Credentials; outsider: Credentials; organizationId: Id; projectId: Id; taskId?: Id; ownerMembershipId: Id; memberMembershipId: Id; adminMembershipId?: Id; }
export interface CorrectnessResult { findings: CorrectnessFinding[]; aborted: boolean; abortReason?: string; }
export class BenchmarkOperationError extends Error { readonly classification: FindingClassification; readonly code?: string; readonly status?: number; constructor(classification: FindingClassification, detail:{code?:string;status?:number}={}) { super(detail.code||classification); this.name="BenchmarkOperationError"; this.classification=classification; this.code=detail.code; this.status=detail.status; } }
export function classifyOperationError(error: unknown): FindingClassification { const e=error as Partial<BenchmarkOperationError>&{code?:string;status?:number}; if(e instanceof BenchmarkOperationError)return e.classification; if(e.status===401)return "authentication"; if(e.status===403)return "authorization"; if(e.status===408||e.code==="timeout")return "timeout"; return "transport/sdk"; }
const names=["valid-sign-in","invalid-sign-in","profile-read-update","task-crud-pagination","comments-crud-pagination","member-tenant-access","outsider-read-isolated","outsider-write-isolated","member-role-denied","admin-role-restore","refresh-signout","required-data"];
export async function runCorrectness(backend:Backend, f:CorrectnessFixture):Promise<CorrectnessResult>{const findings:CorrectnessFinding[]=[];let aborted=false,abortReason:string|undefined;
 const add=async(name:string,work:()=>Promise<void>)=>{if(aborted)return;try{await work();findings.push({name,passed:true,classification:"application",message:"passed"});}catch(e){const x=e as any;const c=classifyOperationError(e);findings.push({name,passed:false,classification:c,message:"check failed",evidence:x?.code||x?.status?String(x.code||x.status):undefined});if(c==="backend_health"){aborted=true;abortReason="backend health lost";}}};
 let owner:AppSession|undefined, admin:AppSession|undefined, member:AppSession|undefined, outsider:AppSession|undefined;
 try { await add("valid-sign-in",async()=>{owner=await backend.createSession(f.owner);await owner.getProfile()}); await add("invalid-sign-in",async()=>{try{await backend.createSession({email:f.owner.email,password:"invalid"});throw new BenchmarkOperationError("application",{code:"accepted_invalid_password"});}catch(e){if((e as any).classification!=="authentication")throw e;}}); if(aborted)return {findings,aborted,abortReason};
 await add("profile-read-update",async()=>{const p=await owner!.getProfile();if(!p.id||!p.email)throw new BenchmarkOperationError("invalid_response",{code:"profile_fields"});await owner!.updateProfile({displayName:"Owner checked"})});
 await add("task-crud-pagination",async()=>{const t=await owner!.createTask({organizationId:f.organizationId,projectId:f.projectId,title:"check",description:"check",priority:"low"});if(!t.id||t.projectId!==f.projectId||!t.title||!t.createdAt)throw new BenchmarkOperationError("invalid_response",{code:"task_fields"});const got=await owner!.getTask({organizationId:f.organizationId,projectId:f.projectId,taskId:t.id,comments:{page:0,pageSize:10}});if(got.task.id!==t.id||got.task.projectId!==f.projectId||got.comments.page!==0||got.comments.pageSize!==10||got.comments.total<0)throw new BenchmarkOperationError("invalid_response",{code:"task_detail_fields"});const updated=await owner!.updateTask({organizationId:f.organizationId,projectId:f.projectId,taskId:t.id,title:"updated"});if(updated.id!==t.id||updated.title!=="updated")throw new BenchmarkOperationError("invalid_response",{code:"update_return"});const a=await owner!.listTasks({organizationId:f.organizationId,projectId:f.projectId,page:0,pageSize:1}),b=await owner!.listTasks({organizationId:f.organizationId,projectId:f.projectId,page:1,pageSize:1}),filtered=await owner!.listTasks({organizationId:f.organizationId,projectId:f.projectId,status:"done",page:0,pageSize:10});if(a.page!==0||a.pageSize!==1||a.total< a.items.length||a.hasNext!== (a.total>1)||filtered.items.some(x=>x.status!=="done")||new Set([...a.items,...b.items].map(x=>x.id)).size!==a.items.length+b.items.length)throw new BenchmarkOperationError("invalid_response",{code:"pagination"})});
 let commentId=""; await add("comments-crud-pagination",async()=>{const c=await owner!.addComment({organizationId:f.organizationId,projectId:f.projectId,taskId:f.taskId||"task-1",body:"check"});commentId=c.id;if(!c.id||c.taskId!==(f.taskId||"task-1")||!c.authorId||!c.createdAt)throw new BenchmarkOperationError("invalid_response",{code:"comment_fields"});const d=await owner!.getTask({organizationId:f.organizationId,projectId:f.projectId,taskId:c.taskId,comments:{page:0,pageSize:10}});if(!d.comments.items.some(x=>x.id===c.id)||d.comments.total<1)throw new BenchmarkOperationError("invalid_response",{code:"comment_missing"});await owner!.updateComment({organizationId:f.organizationId,projectId:f.projectId,taskId:c.taskId,commentId:c.id,body:"updated"});const after=await owner!.getTask({organizationId:f.organizationId,projectId:f.projectId,taskId:c.taskId,comments:{page:0,pageSize:10}});if(after.comments.items.find(x=>x.id===c.id)?.body!=="updated")throw new BenchmarkOperationError("invalid_response",{code:"comment_update_return"})});
 await add("member-tenant-access",async()=>{member=await backend.createSession(f.member);await member.getProfile();await member!.listTasks({organizationId:f.organizationId,projectId:f.projectId,page:0,pageSize:10})}); await add("outsider-read-isolated",async()=>{outsider=await backend.createSession(f.outsider);try{await outsider.listTasks({organizationId:f.organizationId,projectId:f.projectId,page:0,pageSize:10});throw new BenchmarkOperationError("authorization",{code:"outsider_read_allowed"});}catch(e){if((e as any).code!=="tenant_denied")throw e;}}); await add("outsider-comment-read-isolated",async()=>{try{await outsider!.getTask({organizationId:f.organizationId,projectId:f.projectId,taskId:f.taskId||"task-1",comments:{page:0,pageSize:10}});throw new BenchmarkOperationError("authorization",{code:"outsider_comment_read_allowed"});}catch(e){if((e as any).code!=="tenant_denied")throw e;}}); await add("outsider-write-isolated",async()=>{try{await outsider!.createTask({organizationId:f.organizationId,projectId:f.projectId,title:"x",description:"x",priority:"low"});throw new BenchmarkOperationError("authorization",{code:"outsider_write_allowed"});}catch(e){if((e as any).code!=="tenant_denied")throw e;}});
 await add("outsider-comment-write-isolated",async()=>{try{await outsider!.addComment({organizationId:f.organizationId,projectId:f.projectId,taskId:f.taskId||"task-1",body:"x"});throw new BenchmarkOperationError("authorization",{code:"outsider_comment_write_allowed"});}catch(e){if((e as any).code!=="tenant_denied")throw e;}}); await add("member-role-denied",async()=>{try{await member!.updateMembershipRole({organizationId:f.organizationId,membershipId:f.memberMembershipId,role:"admin"});throw new BenchmarkOperationError("authorization",{code:"member_role_allowed"});}catch(e){if((e as any).code!=="role_denied")throw e;}}); await add("admin-role-restore",async()=>{if(f.admin)admin=await backend.createSession(f.admin); const a=admin||owner!; const m=await a.updateMembershipRole({organizationId:f.organizationId,membershipId:f.memberMembershipId,role:"admin"});if(m.role!=="admin")throw new BenchmarkOperationError("invalid_response",{code:"role"});await a.updateMembershipRole({organizationId:f.organizationId,membershipId:f.memberMembershipId,role:"member"})}); await add("refresh-signout",async()=>{await owner!.refreshSession();await owner!.signOut();try{await owner!.getProfile();throw new BenchmarkOperationError("authentication",{code:"post_signout_allowed"});}catch(e){if((e as any).code!=="signed_out")throw e;}}); await add("required-data",async()=>{if(!f.organizationId||!f.projectId||!f.memberMembershipId)throw new BenchmarkOperationError("invalid_response",{code:"fixture_ids"})});
 } catch(e){aborted=true;abortReason=(e as Error).message;} finally {await Promise.all([owner?.close(),admin?.close(),member?.close(),outsider?.close()]);} return {findings,aborted,abortReason}; }
