import os from "node:os";
import process from "node:process";
import { execFile } from "node:child_process";
import { promisify } from "node:util";
import { monitorEventLoopDelay } from "node:perf_hooks";
import type { BackendInfo } from "./backend.js";

export type Optional<T> = { value: T | null; reason?: string };
export interface PsProcess { pid:number; cpuPercent:number; rssBytes:number }
export interface DockerStat { containerId:string; cpuPercent:number; memoryBytes:number; blockReadBytes:number; blockWriteBytes:number }
export interface SysctlInfo { model:string|null; logicalCores:number|null; memoryBytes:number|null }
const unavailable = (reason:string): null => null;
const finite = (n:number) => Number.isFinite(n) && n >= 0;
export function parseByteUnit(input:string): number|null {
  const m=input.trim().match(/^([0-9]+(?:\.[0-9]+)?)\s*(B|KiB|MiB|GiB|TiB|KB|MB|GB|TB|K|M|G|T)$/i); if(!m) return null;
  const n=Number(m[1]); const raw=m[2]!.toUpperCase(); const u=raw.length===1&&raw!=="B"?raw+"IB":raw; const binary=["B","KIB","MIB","GIB","TIB"].includes(u); const p=binary?1024:1000; const i=["B",binary?"KIB":"KB",binary?"MIB":"MB",binary?"GIB":"GB",binary?"TIB":"TB"].indexOf(u); const v=n*p**i; return Number.isSafeInteger(v)?v:null;
}
export function parseSysctl(text:string):SysctlInfo { let model:string|null=null, logicalCores:number|null=null, memoryBytes:number|null=null; for(const line of text.split(/\r?\n/)){const m=line.match(/^\s*([^:=]+)\s*[:=]\s*(.*?)\s*$/);if(!m)continue;const k=m[1]!.toLowerCase(),v=m[2]!;if(/machdep.cpu.brand_string|hw.model/.test(k))model=v||null;else if(/logicalcpu|ncpu|processor/.test(k)){const n=Number(v);if(Number.isSafeInteger(n)&&n>0)logicalCores=n;}else if(/memsize|memory/.test(k)){const n=Number(v);if(Number.isSafeInteger(n)&&n>0)memoryBytes=n;}} return {model,logicalCores,memoryBytes}; }
export function parsePs(text:string):PsProcess[]{const out:PsProcess[]=[];for(const l of text.split(/\r?\n/)){const p=l.trim().split(/\s+/);if(p.length<3)continue;const pid=Number(p[0]),cpu=Number(p[1]),rss=parseByteUnit(p.slice(2).join(" ")) ?? (Number(p[2])*1024);if(Number.isSafeInteger(pid)&&pid>0&&finite(cpu)&&rss!==null&&finite(rss))out.push({pid,cpuPercent:cpu,rssBytes:rss});}return out;}
export function parseDockerStats(text:string):DockerStat[]{const out:DockerStat[]=[];for(const l of text.split(/\r?\n/)){try{const x=JSON.parse(l);const cpu=Number(String(x.CPUPerc??"").replace("%",""));const mem=String(x.MemUsage??"").split("/")[0];const io=String(x.BlockIO??"").split("/");const a=parseByteUnit(mem),r=parseByteUnit(io[0]??""),w=parseByteUnit(io[1]??"");if(typeof x.ID==="string"&&/^[0-9a-f]+$/i.test(x.ID)&&finite(cpu)&&a!==null&&r!==null&&w!==null)out.push({containerId:x.ID,cpuPercent:cpu,memoryBytes:a,blockReadBytes:r,blockWriteBytes:w});}catch{}}return out;}
export interface RunnerSnapshot { timestampNs:number; runner:{cpuPercent:number|null;rssBytes:number|null}; eventLoop:{p99Ms:number|null;maxMs:number|null}; backend:Record<string,unknown> }
export function evaluateRunnerOverload(samples:RunnerSnapshot[], thresholds:{cpuPercent?:number;p99Ms?:number;maxMs?:number;consecutiveSamples?:number}={}):string|null {const n=thresholds.consecutiveSamples??3;if(!Number.isSafeInteger(n)||n<1)throw Error("consecutiveSamples must be positive");if(samples.length<n)return null;const tail=samples.slice(-n);if(tail.some(s=>s.runner.cpuPercent===null||s.eventLoop.p99Ms===null||s.eventLoop.maxMs===null))return "runner metrics unavailable";if(thresholds.cpuPercent!==undefined&&tail.every(s=>s.runner.cpuPercent!>thresholds.cpuPercent!))return "runner CPU sustained above threshold";if(thresholds.p99Ms!==undefined&&tail.every(s=>s.eventLoop.p99Ms!>thresholds.p99Ms!))return "event-loop p99 sustained above threshold";if(thresholds.maxMs!==undefined&&tail.every(s=>s.eventLoop.maxMs!>thresholds.maxMs!))return "event-loop max sustained above threshold";return null;}
export interface Environment { os:string; release:string; arch:string; cpuModel:string|null; logicalCores:number|null; totalMemoryBytes:number|null; hostname:string; nodeVersion:string; npmVersion:string|null; gitCommit:string|null; gitDirty:boolean|null; backend:BackendInfo; sdkVersion:string|null; dockerVersion?:string|null; supabaseVersion?:string|null }
export interface CommandResult { stdout:string; stderr:string }
export type CommandRunner=(command:string,args:string[],timeoutMs:number)=>Promise<CommandResult>;
const defaultRunner:CommandRunner=(command,args,timeoutMs)=>new Promise((resolve,reject)=>{execFile(command,args,{timeout:timeoutMs,maxBuffer:64*1024},(error,stdout,stderr)=>error?reject(new Error("command failed")):resolve({stdout:String(stdout),stderr:String(stderr)}));});
export async function captureEnvironment(backend:BackendInfo,sdkVersion?:string,runner:CommandRunner=defaultRunner):Promise<Environment>{const run=async(c:string,a:string[])=>{try{return (await runner(c,a,3000)).stdout.trim();}catch{return ""}};const sys=process.platform==="darwin"?parseSysctl(await run("sysctl",["-a"])):parseSysctl(await run("cat",["/proc/cpuinfo"]));const commit=await run("git",["rev-parse","HEAD"]);const dirty=await run("git",["status","--porcelain"]);const npm=await run("npm",["--version"]);return {os:process.platform,release:os.release(),arch:process.arch,cpuModel:sys.model,totalMemoryBytes:sys.memoryBytes??(Number.isSafeInteger(os.totalmem())?os.totalmem():null),logicalCores:sys.logicalCores??os.cpus().length,hostname:os.hostname(),nodeVersion:process.version,npmVersion:/^\d+\.\d+\.\d+$/.test(npm)?npm:null,gitCommit:/^[0-9a-f]{40}$/i.test(commit)?commit:null,gitDirty:dirty!==""?true:(commit?false:null),backend,sdkVersion:sdkVersion??null};}
